<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd" >
<configuration>

	<settings>
		<!-- 일부 데이터베이스 제품은 컬럼에 null 값이 저장되는 경우 해당 컬럼의 jdbc 타입을 요구한다. 아래의 설정은 JDBC 
			타입과 상관없이 null 값이 저장되도록 한다. 쓰지 않았을 때는 #{tel, jdbcType=VARCHAR } 와 같이 일일이 써줘야한다. -->
		<setting name="jdbcTypeForNull" value="NULL" />
		<!-- 조회된 컬럼의 값이 null 인경우라도 setter 메소드나 , Map의 put 메소드를 호출하게 한다. resultType="hashmap"으로 
			설정하는 경우 필요한 설정 * int , boolean 과 같은 기본자료형의 변수인 null 값이 대입되지 않는 문제가있다. -->
		<setting name="callSettersOnNulls" value="false" />

	</settings>

</configuration>